{
  "name": "story-quiz-bot",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "postinstall": "npm install --prefix ./server",
    "start": "npm run start --prefix ./server",
    "deploy": "npm run build --prefix ./client && npm run build --prefix ./panel"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
